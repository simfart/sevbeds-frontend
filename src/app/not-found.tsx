import { NotFoundPage } from '@/screens/NotFound/NotFoundPage';

/**
 * not-found.tsx — специальный файл Next.js. Показывается при 404
 * (несуществующий URL или вызов notFound() из кода).
 */
export default function NotFound() {
  return <NotFoundPage />;
}
